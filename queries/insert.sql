select 'insert';

-- regular insert method
-- column list can be omitted but values for all columns must be specified
-- inserting multiple rows
-- inserting default values (insert into table_name default values)
-- if no default is specified in the table schema null is used
-- inserting rows from the result of a select statement
